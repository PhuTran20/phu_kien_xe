const { functions } = require("lodash");

window.addEventListener("load", function () {
    const dropdown = document.querySelector(".dropdown");
    const d
    dropdown.addEventListener("click",functions(e){

    })
});
